/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['scholar.rotaractmora.org','upload.wikimedia.org','avatars.githubusercontent.com'],
  },
}

module.exports = nextConfig